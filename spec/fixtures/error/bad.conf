		== THIS IS NOT A YAML FILE!! ==
This is the kind of file that someone would make if
they are not actually writing YAML. It is important
that an error is caused by this file when we try to
parse it as a YAML file using Ruby's YAML parser!!!
